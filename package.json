{
  "name": "personal-blog",
  "private": true,
  "version": "1.0.0",
  "description": "A minimalistic personal blog built from markdown files, deployed to GitHub Pages.",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx --yes serve dist"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
